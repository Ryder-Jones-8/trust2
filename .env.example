# trust. Platform Environment Configuration
# Copy this file to .env.local for local network access

# For local development (localhost only)
# VITE_API_URL=http://localhost:3001

# For network access (replace with your actual IP)
# Get your IP by running: ipconfig (Windows) or ifconfig (Mac/Linux)
# VITE_API_URL=http://192.168.1.XXX:3001

# Production settings
NODE_ENV=development
VITE_PLATFORM_NAME=trust.

# Backend will automatically bind to 0.0.0.0:3001 for network access
# Frontend will automatically bind to 0.0.0.0:5173 for network access
